#!/usr/bin/env node
/* eslint-disable no-var */
var path = require('path')

require('source-map-support').install()

require('app-module-path').addPath(
  path.resolve(path.join(__dirname, '..', 'lib'))
)

require('foreman').init()
